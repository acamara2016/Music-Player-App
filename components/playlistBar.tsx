import { Box } from "@chakra-ui/layout";
type PlaylistBarProps = {
  song: ISong;
};
const PlaylistBar: React.FC<PlaylistBarProps> = ({ song }) => {
  return <Box></Box>;
};

export default PlaylistBar;
